# Regenerable P toolchain outputs (p compile / p check). The sweep
# summaries the calibration logs cite as evidence are force-added.
PGenerated/
PCheckerOutput/
# Machine-replay counterexample archives (~200k lines across the
# calibrated reds). Regenerate any cell's counterexample with
# `p check -tc <cell>` per the run log; the committed evidence is the
# sweep summaries.
trace.json
# Occult engine solver checkpoints (regenerable tool output, ~1MB of
# base64 solver state per probe run).
*.checkpoint.json
